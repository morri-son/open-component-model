{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/sigstore/signing/v1alpha1/schemas/VerifyConfig.schema.json",
  "title": "VerifyConfig",
  "type": "object",
  "description": "VerifyConfig defines configuration for Sigstore-based verification.\n\n# Trusted Root Discovery\n\nTrusted root material is resolved from credentials, TrustedRootPath,\nTUFRootURL, or (as a fallback) the public-good Sigstore TUF repository.\nFor key-based verification, TUF auto-discovery is skipped when no explicit\nsource is configured — the public key alone is sufficient.\n\n# Verification Modes\n\nKeyless verification requires at least one identity field (ExpectedIssuer, ExpectedSAN,\nor their regex variants). The Fulcio certificate in the bundle is validated against the\ntrusted root's CA key and the identity constraints. No external key material is needed.\n\nKey-based verification requires the public key to be provided out of band via credentials,\nsince the bundle only contains a hint. Without the public key, verification fails.\n\nTransparency log and timestamp requirements are auto-detected from the trusted material:\nif the trusted root contains Rekor log entries, transparency log verification is enforced;\nif it contains timestamping authorities, observer timestamps are required; otherwise,\nintegrated timestamps (Rekor v1 SETs) are expected.",
  "properties": {
    "expectedIssuer": {
      "type": "string",
      "description": "ExpectedIssuer is the exact OIDC issuer URL to verify against in keyless signatures.\nUsed during verification to match the Fulcio certificate's issuer extension."
    },
    "expectedIssuerRegex": {
      "type": "string",
      "description": "ExpectedIssuerRegex is a regular expression to match the OIDC issuer in keyless signatures.\nMutually exclusive with ExpectedIssuer — if both are set, ExpectedIssuer takes precedence."
    },
    "expectedSAN": {
      "type": "string",
      "description": "ExpectedSAN is the exact Subject Alternative Name to verify against in keyless signatures.\nThis is the identity (email or URI) embedded in the Fulcio certificate."
    },
    "expectedSANRegex": {
      "type": "string",
      "description": "ExpectedSANRegex is a regular expression to match the Subject Alternative Name.\nMutually exclusive with ExpectedSAN — if both are set, ExpectedSAN takes precedence."
    },
    "trustedRootPath": {
      "type": "string",
      "description": "TrustedRootPath is a filesystem path to a trusted root JSON file for offline verification.\nWhen set, this takes precedence over TUF-based root fetching."
    },
    "tufInitialRoot": {
      "type": "string",
      "description": "TUFInitialRoot is the initial (pinned) TUF root.json used to bootstrap trust\nwhen fetching from a custom TUF mirror (TUFRootURL). This is mandatory when\nTUFRootURL is set — the TUF security model requires a known-good initial root\nto verify all subsequent metadata. The value is raw JSON (not base64-encoded)."
    },
    "tufRootURL": {
      "type": "string",
      "description": "TUFRootURL is the URL of a TUF repository for fetching trusted root material.\nWhen empty, TUF is not used; provide a trusted root via credentials or TrustedRootPath instead.\nWhen set, TUFInitialRoot must also be provided as the trust anchor."
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "description": "Type identifies this configuration object's runtime type.",
      "oneOf": [
        {
          "const": "SigstoreVerificationConfiguration/v1alpha1"
        },
        {
          "deprecated": true,
          "const": "SigstoreVerificationConfiguration"
        }
      ]
    }
  },
  "required": [
    "type"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    }
  }
}