{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "generated by the ocm schema generation tool",
  "$id": "ocm.software/open-component-model/bindings/go/sigstore/signing/v1alpha1/schemas/Config.schema.json",
  "title": "Config",
  "type": "object",
  "description": "Config defines configuration for Sigstore-based signing and verification.\n\nWhen all URL fields are empty, the public Sigstore infrastructure is used by default.\nCustom infrastructure can be configured by setting the endpoint URLs.",
  "properties": {
    "expectedIssuer": {
      "type": "string",
      "description": "ExpectedIssuer is the exact OIDC issuer URL to verify against in keyless signatures.\nUsed during verification to match the Fulcio certificate's issuer extension."
    },
    "expectedIssuerRegex": {
      "type": "string",
      "description": "ExpectedIssuerRegex is a regular expression to match the OIDC issuer in keyless signatures.\nMutually exclusive with ExpectedIssuer — if both are set, ExpectedIssuer takes precedence."
    },
    "expectedSAN": {
      "type": "string",
      "description": "ExpectedSAN is the exact Subject Alternative Name to verify against in keyless signatures.\nThis is the identity (email or URI) embedded in the Fulcio certificate."
    },
    "expectedSANRegex": {
      "type": "string",
      "description": "ExpectedSANRegex is a regular expression to match the Subject Alternative Name.\nMutually exclusive with ExpectedSAN — if both are set, ExpectedSAN takes precedence."
    },
    "forceTSA": {
      "type": "boolean",
      "description": "ForceTSA forces the use of a Timestamp Authority even when Rekor is available.\nBy default, Rekor's integrated timestamps are used. When set, a TSA timestamp is also obtained."
    },
    "fulcioURL": {
      "type": "string",
      "description": "FulcioURL is the URL of the Fulcio certificate authority for keyless signing.\nIf empty, the public Sigstore Fulcio instance is used."
    },
    "rekorURL": {
      "type": "string",
      "description": "RekorURL is the URL of the Rekor transparency log.\nIf empty, the public Sigstore Rekor instance is used."
    },
    "rekorVersion": {
      "type": "integer",
      "description": "RekorVersion specifies the Rekor API version to use (1 or 2).\nWhen 0 (default), version 1 is used for direct URL mode.\nWhen a SigningConfigPath is provided, the highest available version is auto-selected.",
      "minimum": 0,
      "maximum": 4294967295
    },
    "signingConfigPath": {
      "type": "string",
      "description": "SigningConfigPath is a filesystem path to a signing_config.json file.\nWhen set, service endpoints (Fulcio, Rekor, TSA) are discovered from this file\ninstead of using the individual URL fields. This is the standard Sigstore mechanism\nfor service endpoint discovery and supports automatic Rekor v2 selection."
    },
    "skipRekor": {
      "type": "boolean",
      "description": "SkipRekor disables transparency log integration.\nWhen true, signatures are not uploaded to Rekor and Rekor entries are not required for verification."
    },
    "trustedRootPath": {
      "type": "string",
      "description": "TrustedRootPath is a filesystem path to a trusted root JSON file for offline verification.\nWhen set, this takes precedence over TUF-based root fetching."
    },
    "tsaURL": {
      "type": "string",
      "description": "TSAURL is the URL of a RFC 3161 Timestamp Authority.\nIf empty, no timestamp authority is used unless ForceTSA is set."
    },
    "tufRootURL": {
      "type": "string",
      "description": "TUFRootURL is the URL of a TUF repository for fetching trusted root material.\nIf empty, the default Sigstore TUF root is used."
    },
    "type": {
      "$ref": "#/$defs/ocm.software.open-component-model.bindings.go.runtime.Type",
      "description": "Type identifies this configuration object's runtime type.",
      "oneOf": [
        {
          "const": "SigstoreSigningConfiguration/v1alpha1"
        },
        {
          "deprecated": true,
          "const": "SigstoreSigningConfiguration"
        }
      ]
    }
  },
  "required": [
    "type"
  ],
  "additionalProperties": false,
  "$defs": {
    "ocm.software.open-component-model.bindings.go.runtime.Type": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$comment": "this core runtime schema was automatically included by the ocm schema generation tool to allow introspection",
      "title": "Type",
      "type": "string",
      "description": "Type represents a structured type with an optional version and a name. It is used to identify the type of an object in a versioned API.",
      "pattern": "^([a-zA-Z0-9][a-zA-Z0-9.]*)(?:/(v[0-9]+(?:alpha[0-9]+|beta[0-9]+)?))?$"
    }
  }
}