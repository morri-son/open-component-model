# Theme assets — credits and licensing

Source attribution for images in this directory.

## Hero banner

### `OCM-Banner.png` (active)

- **Source:** banner image curated by the OCM project owner from the
  NeoNephos Foundation visual material (NeoNephos.org). Trimmed and
  laid out for the OCM exec deck.
- **Licence:** internal use; the image originates from NeoNephos
  Foundation material (an LF Europe project the OCM project is part of).
  Treat as **editorial use only**; if used externally, confirm usage
  with NeoNephos before publication.
- **Crop:** the file is composed for full-bleed placement on the Hero
  layout (1920×1080). The build script (`../build-pptx/build_pptx.py`,
  `resolve_hero_banner()`) embeds it as-is.

The companion file lives at
`assets/neonephos/OCM-Banner.png` (the canonical location); a copy is
mirrored to `theme/OCM-Banner.png` so the build script can find it from
the deck workspace.
